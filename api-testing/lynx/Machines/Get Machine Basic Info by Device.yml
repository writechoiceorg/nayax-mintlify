info:
  name: Get Machine Basic Info by Device
  type: http
  seq: 7

http:
  method: GET
  url: https://qa-lynx.nayax.com/operational/v1/devices/{DeviceSerialNumber}/machine
  auth:
    type: bearer
    token: {{NAYAX_TOKEN}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 OK — response not yet captured, re-run to save
    request:
      url: https://qa-lynx.nayax.com/operational/v1/devices/{DeviceSerialNumber}/machine
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json; charset=utf-8
      body:
        type: text
        data: ""
