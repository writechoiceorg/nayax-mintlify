info:
  name: Delete Actor Group
  type: http
  seq: 20

http:
  method: DELETE
  url: https://qa-lynx.nayax.com/operational/v1/actors/{ActorID}/roleGroups
  auth:
    type: bearer
    token: {{NAYAX_TOKEN}}
  body:
    type: json
    data: |
      [
        {
          "RoleGroupId": 0
        }
      ]

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: SKIP — not run during testing to avoid side effects
    request:
      url: https://qa-lynx.nayax.com/operational/v1/actors/{ActorID}/roleGroups
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
