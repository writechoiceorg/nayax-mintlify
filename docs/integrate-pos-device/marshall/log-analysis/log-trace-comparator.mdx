---
title: "Log Trace Comparator Tool"
metadata:
  og:description: "Check a Marshall SDK or firmware log against known-good reference traces, entirely in your browser, and get a pass/fail verdict with suggested fixes."
---

import { MarshallLogTool } from "/snippets/MarshallLogTool.jsx";

Use this tool to check a Marshall SDK or firmware log against known-good reference traces without reviewing it line by line. Drop in either of the following:

- A log file (`.txt` or `.log`)
- An Excel certification workbook, where each sheet is treated as one test

The tool runs it through the same checkpoints Nayax uses when reviewing your certification tests: boot handshake, reader state, vend and authorization flow, and session close.

<Note>
Everything runs locally in your browser. Log files never leave your machine, except for the "Learn more" links, which open Nayax's documentation.
</Note>

## Reading the results

Each log or sheet gets a verdict badge:

- <Badge color="green">ALL CHECKS PASSED</Badge>: every checkpoint passed, no action needed.
- <Badge color="orange">REVIEW RECOMMENDED</Badge>: at least one checkpoint raised a warning worth a manual look. Not necessarily a bug.
- <Badge color="red">FAILED CHECKS FOUND</Badge>: at least one checkpoint failed outright and needs attention.

Below the badge, a trace strip shows one dot per checkpoint, in the same colors:

- <Badge color="green">Green</Badge>: pass
- <Badge color="orange">Amber</Badge>: warning
- <Badge color="red">Red</Badge>: fail
- <Badge color="gray" variant="outline">Hollow gray</Badge>: not applicable to that log

For any checkpoint that isn't green, a "Suggested fix" box explains the likely cause and links to the relevant documentation, and an evidence line shows the exact log line it was based on.

<Tip>
If a test includes an intentional long wait, such as a 24-hour idle test, a session timeout, or a stress test, raise the timing anomaly threshold before running it. Otherwise the tool reports a FAIL on timing that's actually the test working as designed.
</Tip>

<MarshallLogTool />

## See also

<CardGroup cols={2}>
  <Card icon="file-lines" title="SDK Log Generation" href="/docs/integrate-pos-device/marshall/additional-resources/log-generation">
    How to turn on SDK logging so you have a log to run through this tool.
  </Card>

  <Card icon="book-open" title="Log Trace Comparator Guide" href="/docs/integrate-pos-device/marshall/additional-resources/log-trace-comparator-guide">
    The full checkpoint reference, scenario tags, and how to tell a real issue from a false positive.
  </Card>
</CardGroup>
